/**
 * Icon re-exports from @carbon/icons-react.
 *
 * For the full library (2600+ icons), import directly:
 *   import { SomeCarbonIcon } from "@carbon/icons-react";
 *
 * This barrel provides backward-compatible aliases (IconAdd, IconClose)
 * and commonly used icons for convenience.
 */

// Backward-compatible aliases
export { Add as IconAdd, Close as IconClose } from "@carbon/icons-react";

// Re-export the full Carbon library for convenience
export * from "@carbon/icons-react";
